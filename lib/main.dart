import 'package:flutter/material.dart';

void main() => runApp(MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: Text('Mobile Build')),
        body: Center(child: Text('Hello World from Mobile!')),
      ),
    ));
