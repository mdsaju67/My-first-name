workflows:
  android-workflow:
    name: Android Build
    instance_type: mac_mini_m1
    environment:
      flutter: stable
    scripts:
      - name: Build APK
        script: flutter build apk --release --debug
    artifacts:
      - build/app/outputs/flutter-apk/*.apk
